#include <stdio.h>

int main(){
    int a;
    scanf("%d", &a);  // the 'scanf' function is used to take input from user..

    /* float a and "%f" for Real variables.
    char x and " %c" for character variables. */
    printf("The value of a is: %d \n", a);  
    // agr '\n' nhi lga rha toh line ke end mein ek % mil rha hae..
    return 0;
}
