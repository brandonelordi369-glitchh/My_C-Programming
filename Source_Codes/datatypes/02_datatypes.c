# include <stdio.h>

int main() {
    int a = 2;

    float b = 3.9;

    char c;
    c = 'B'; // "" (double-quote) is not accecptable..

    printf("%d %f %c", a, b, c);
    // %d %f %c  are called 'format specifiers..
    return 0;
}